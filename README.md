# Worker Hostname Sync

## API Token Scopes

TODO

## Deployment

- Install Terraform: https://developer.hashicorp.com/terraform/install
- Copy `terraform.tfvars.example` to `terraform.tfvars`
- Edit values
- `terraform init`
- `terraform apply`